name: Docker Image CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
    
jobs:
  example:
    runs-on: ubuntu-latest
    steps:
      - name: Testing the trigger.
        if: contains(github.event.comment.body, 'amir')
        run: echo "This is amir comment"

