name: Docker Image CI

on:
  push:
    branches: [ "main" ]
    
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
       - uses: actions/checkout@v3
       - name: My first step ${{ github.event.comment.body }}



